package com.paliy_dmitriy.domain.repository.favorites

import kotlinx.coroutines.flow.Flow

interface FavoriteRepository {
//  suspend fun getFavorites(): List<FavoriteQuote>
//  suspend fun addToFavorites(quote: Quote): Boolean
//  suspend fun removeFromFavorites(quoteId: String): Boolean
//  fun observeFavorites(): Flow<List<FavoriteQuote>>
//  suspend fun isFavorite(quoteId: String): Boolean
}